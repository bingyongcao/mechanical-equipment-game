# 第二场景：装沙技术原型

入口：选择「砂料厂 · 装车」。周围为低矮厂房和道路，车停在北侧围墙外。墙内有 28 × 22 米、初始厚度 0.6 米的沙层（369.6 m³），外围留有窄边缘。使用已有 Blender 自卸卡车，车厢容积 12.32 m³，目标为 9.856 m³（80%）。

## 已实现的计算路径

1. 沙层使用 0.5 米有限体积网格，深度存在 Float64Array 中。渲染与碰撞使用同一列高，挖空后露出不可挖掘硬底。沙坡按 34° 休止角做相邻单元体积交换。
2. 移动或转动的铲刃接近沙层时，局部沙在原位置从地形转为粗颗粒刚体。转换前扣除完全相同体积；没有吸附到铲斗或给载荷绑定父节点。
3. Rapier 在 60 Hz 固定时间步计算颗粒重力、摩擦、碰撞、CCD。使用固定堆积率 0.64 和散装密度 1600 kg/m³；一个颗粒代表一小份沙，不是真实尺寸的单个沙粒。
4. 完全脱离斗内且在沙面附近低速停稳 0.7 秒的颗粒，按原体积沉积回地形或车厢。车厢沙堆有自己的网格、体积及碰撞。超过车厢高度的颗粒不并入沙堆，保留动态状态；道路上的沙也保留颗粒状态。
5. 车厢沉积体积 / 12.32 达到 80%，持续 2 秒，触发一次完成事件。空中、斗内、道路上的沙不计分。异常越界物料只记录到 escaped 账目，不送回车厢、不加分。
6. 体积账本始终核对：初始沙量 = 地面沙量 + 动态颗粒 + 车厢沉积 + 越界损失。乘固定密度即质量账本。

## 验证方法

启动本地 Vite（5173）后执行 `npm.cmd run test:sand`。详细报告与图片输出到 `test-results/sand-report.json`、`sand-yard.png`、`sand-completion.png`。

- 实际搬运测试通过与玩家相同的 `Machine.step` 输入完成下挖、推进、收斗、抬臂、行走、斗杆抬高、回转、翻斗。不修改颗粒位置或装载分数。
- 一次实测挖起约 0.616 m³，卸入车厢后留存约 0.571 m³；铲斗卸空。精确记录以 JSON 为准。
- 独立满载夹具从沙层扣除 11 m³，其中 1 m³ 释放在道路上、10 m³ 分批在车厢上方释放。经过实际碰撞与停稳后计分，最终达到 81.1% 并显示完成 UI。前九批保持未完成。
- 检查完成事件不重复、重开归零、返回展厅解除沙场控制钩子、体积误差 < 1e-6 m³、浏览器没有异常。
- 本机无头 Chromium 两次测试物理步耗时中位数约 2–6.9 ms，95 分位约 13.2–17.1 ms，最大约 24.1–47 ms。仅为该设备、该输入序列的物理耗时，不是帧率保证，也不包含完整渲染时间；报告每次重新测量。

满载夹具验证计量与完成链路，**不代表已用机械控制完成连续二十次铲装**，也不代表所有姿态和满载情况下的装料路径已被穷举验证。

## 操作提示

初始位置可放低动臂、前进、收斗，再抬臂。运到卡车旁时，需要同时抬动臂并伸出斗杆，提高铲斗位置，再回转越墙。铲斗接近车厢边缘会被阻挡，先调整高度。翻斗并逐渐调整斗杆可让沙落下。沙场关闭旧版仅针对平地石堆的「贴地铲装」辅助。

## 原型边界

- 这是守恒的粗粒度游戏物理原型。0.5 米沙层网格可见阶梯，颗粒约厘米到分米级的物料包；没有真实沙粒形状、连续剪切、黏聚、湿度或压实模型。
- 沙层到颗粒转换只在铲刃附近进行；网格转换会有局部松胀和接触修正，并非工程级土壤切削解算。未模拟挖掘阻力、液压功率、颗粒对整机的反作用力或倾覆。
- 履带采用六点最高支撑高度，保持底盘水平。高度能随坑槽变化，但没有真实履带沉陷、姿态倾斜和压实。工作装置与墙/车的阻挡使用保守包围盒，可能提前阻挡。
- 车厢沉积沙视为固定堆积，不能再次挖出。卡车固定停车；车轮、镜子等细节没有完整碰撞体。
- 活动颗粒上限 1600；接近上限时暂停继续激活沙层，并限制继续进入沙层。大量道路散落颗粒可能占满预算，需要重开；当前不承诺任意长时间场景的稳定性能。
- 地形松弛采用封闭网格，不支持沙堆从网格边缘流出。车厢溢出主要由未沉积的活动颗粒处理。

结论：挖掘 → 搬运 → 越墙卸料、有限沙量守恒、车厢体积计量与完成 UI 已有可执行验证。距离精细沙子观感、完整车辆/土壤动力学和跨设备性能验收仍有明确差距。
