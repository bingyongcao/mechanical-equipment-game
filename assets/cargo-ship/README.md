# 货船

`cargo-ship.glb` 为独立货船模型，包含五个可分离的 `Cargo_1` 至 `Cargo_5` 物资箱。每个箱子有 `A_GrabPoint` 前缀的顶部抓取挂点。船长 14.5 米，甲板顶面约为 1.45 米，导出后 Y 轴向上。

可编辑源文件位于 `../truck-crane/crane-and-ship.blend`；建模、重建和运行时限制详见相邻起重机目录 README。尚未建立游戏物理碰撞体；甲板应单独作为支撑面，避免整船凸包把甲板上方货物区封闭。
